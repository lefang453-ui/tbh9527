# PHP 多站点管理系统（毕业设计）补充方案（无代码版）

## 1. 项目定位与答辩亮点

### 1.1 项目定位
构建一个基于 PHP 的「多站点内容管理与运营平台」，核心目标是让管理员在一个后端系统中完成：
- 多站点统一配置与批量管理；
- 站点模板和域名灵活绑定；
- AI 文章生成、审核、发布的一体化流程；
- 图片素材聚合与按站点分配；
- SEO 友好的 URL 规则和栏目策略。

### 1.2 毕设展示亮点（建议重点强调）
1. **工程完整性**：需求分析 → 架构设计 → 数据库设计 → 核心功能实现 → 测试与部署。
2. **业务复杂性**：多站点、多域名、多模板、多资源映射关系。
3. **创新点**：AI 生成文章 + 自动发布时间策略 + 随机栏目策略。
4. **可运营性**：可视化后台 + 日志审计 + 权限分级。

---

## 2. 功能总览（含你提出的模块 + 建议补充）

### 2.1 后端可视化管理模块
- 仪表盘：站点数量、文章数量、发布趋势、AI 生成成功率。
- 统一登录与权限控制（超级管理员/编辑/运营）。
- 全站消息通知（任务执行状态、失败告警）。

### 2.2 AI 文章生成模块
- 支持按关键词、栏目、站点策略生成文章。
- 提示词模板管理（按行业/栏目维护 Prompt 模板）。
- 文章生成后进入审核池：草稿 → 审核通过 → 定时发布。
- 敏感词检测、重复度检测、字数限制。
- 支持自动配图策略（从图片库按标签匹配）。

### 2.3 网站管理模块（你提出的核心）
每个站点维度支持：
- 站点基础设置（站名、描述、Logo、主题色等）；
- URL 规则设置（伪静态/路径规则/后缀）；
- 泛域名配置（自定义泛域名策略）；
- 重定向设置（301/302，支持路径级规则）；
- 栏目设置（自定义栏目、随机栏目池）；
- 备案号设置（站点级独立备案号）；
- 文章发布时间策略（立即发布/随机延迟/定时批量）。

### 2.4 多域名绑定与模板管理模块
- 单域名绑定模板：一个域名使用一个模板；
- 多域名批量绑定模板：多个域名共用一个模板；
- 模板版本管理（v1/v2，可回滚）；
- 模板启停与预览开关（灰度发布思路）。

### 2.5 图片库管理模块
- 采集知名网站公开图片链接（只存链接元信息，不落地侵权内容）；
- 生成图片链接文本库（按主题/标签分类）；
- 每个站点绑定一个或多个图片链接文本源；
- 文章发布时按站点绑定策略自动抽取图片链接。

### 2.6 建议新增模块（用于“完美毕业作品”）
1. **任务调度中心**：AI生成、定时发布、图片抽取均以任务形式执行。
2. **日志与审计模块**：登录日志、操作日志、发布日志、错误日志。
3. **数据备份与恢复模块**：数据库定时备份、一键恢复演示。
4. **系统设置模块**：邮件/短信通知、API 密钥管理、缓存配置。
5. **统计分析模块**：站点维度文章产出、栏目活跃度、生成耗时。

---

## 3. 推荐技术架构（PHP）

### 3.1 技术栈建议
- 后端框架：Laravel 10（或 ThinkPHP 8，二选一）
- 管理端 UI：Laravel + Blade + AdminLTE（快速成型）
- 数据库：MySQL 8
- 缓存/队列：Redis
- 定时任务：Crontab + 队列 Worker
- Web 服务：Nginx
- 文件对象存储（可选）：本地存储 / MinIO / OSS

### 3.2 分层架构建议
- Controller 层：接收请求、参数校验、返回视图/JSON。
- Service 层：封装业务流程（AI生成、发布策略、模板绑定）。
- Repository/Model 层：数据读写与关系维护。
- Job/Queue 层：异步执行批量任务。
- Policy/RBAC 层：权限与角色控制。

---

## 4. 数据库设计（核心表清单）

### 4.1 基础实体
- `users`：用户表
- `roles`：角色表
- `permissions`：权限表
- `role_user` / `permission_role`：权限关联

### 4.2 站点与域名
- `sites`：站点主表（基础设置、备案号、状态）
- `site_domains`：站点域名表（主域名/泛域名/状态）
- `domain_redirect_rules`：域名重定向规则
- `site_url_rules`：站点 URL 规则

### 4.3 栏目与内容
- `categories`：栏目表（支持父子栏目）
- `site_category_bindings`：站点栏目绑定（含随机池标记）
- `articles`：文章表（来源、状态、发布时间）
- `article_publish_plans`：发布时间策略记录

### 4.4 AI 与素材
- `ai_providers`：AI 服务配置
- `ai_prompt_templates`：提示词模板
- `ai_generation_tasks`：生成任务记录
- `image_link_collections`：图片链接文本集合
- `image_links`：图片链接明细
- `site_image_collection_bindings`：站点与图片链接集合绑定

### 4.5 模板与发布
- `ui_templates`：模板主表
- `template_versions`：模板版本
- `domain_template_bindings`：域名模板绑定

### 4.6 运维与审计
- `operation_logs`：操作日志
- `login_logs`：登录日志
- `system_jobs`：异步任务执行记录
- `system_configs`：系统配置

---

## 5. 关键业务流程（答辩可画流程图）

### 5.1 AI 文章生成与发布流程
1. 管理员选择站点 + 栏目 + 关键词。
2. 系统读取站点策略与 Prompt 模板。
3. 提交 AI 生成任务到队列。
4. 生成结果进入审核池。
5. 审核通过后按发布时间策略入发布任务。
6. 到时自动发布并写日志。

### 5.2 多域名模板绑定流程
1. 录入域名并验证归属。
2. 选择模板版本并绑定域名（单个/批量）。
3. 保存生效策略（立即/延时）。
4. 记录操作日志，可回滚到旧版本。

### 5.3 图片链接文本绑定流程
1. 导入/采集图片链接集合。
2. 按主题打标签。
3. 绑定到站点或站点栏目。
4. 文章发布时按规则选图。

---

## 6. 非功能需求（老师常问）

### 6.1 安全性
- 密码加密存储（bcrypt/argon2）。
- CSRF、防 SQL 注入、XSS 过滤。
- API 密钥加密存储与脱敏展示。
- 敏感操作二次确认（删除/批量替换）。

### 6.2 性能
- 列表分页与索引优化。
- 热数据走 Redis 缓存。
- AI/发布任务异步化，避免阻塞后台。

### 6.3 可维护性
- 统一日志格式。
- 模块化目录结构。
- 关键参数可配置化（而非写死）。

---

## 7. 毕设文档建议章节（可直接用于论文目录）
1. 绪论（背景、意义、研究现状）
2. 需求分析（功能/非功能/用例图）
3. 总体设计（架构、模块划分、流程图）
4. 数据库设计（E-R 图、表结构说明）
5. 系统实现（核心页面与关键逻辑）
6. 系统测试（功能测试、性能测试、安全测试）
7. 总结与展望

---

## 8. 开发里程碑（8 周示例）
- 第1周：需求冻结 + 原型图 + 数据库初稿。
- 第2周：用户权限、站点管理、域名管理。
- 第3周：栏目管理、URL规则、重定向规则。
- 第4周：模板管理、域名模板绑定。
- 第5周：AI生成模块 + 审核流程。
- 第6周：图片链接文本库 + 站点绑定。
- 第7周：任务调度、日志审计、性能优化。
- 第8周：测试、部署、答辩材料整理（PPT/演示脚本）。

---

## 9. 答辩加分建议
- 准备一套演示账号（管理员/编辑）对比权限。
- 演示“批量域名绑定模板”与“AI定时发布”两个高价值场景。
- 展示日志审计（体现工程规范）。
- 对 AI 内容安全给出策略：人工审核 + 敏感词过滤 + 可追溯日志。

---

## 10. 风险与规避
- AI 输出不稳定：使用多模板 + 失败重试 + 最终人工审核。
- 图片版权风险：仅采集可公开引用资源，记录来源，支持手动替换。
- 多站点规则冲突：规则优先级机制（站点级 > 全局级）。
- 发布任务堆积：队列分组 + 并发 worker + 失败告警。

